# Глава 4 — Key Distribution and User Authentication (шпаргалка)

**Цель:** понять, как происходит распределение симметричных ключей и аутентификация пользователей в системах с использованием Kerberos.

---

## 🔹 4.1 Symmetric Key Distribution Using Symmetric Encryption

**Идея:** две стороны (A и B) должны иметь общий секретный ключ, который никто другой не знает. Ключ нужно доставить безопасно и часто менять.

### ▪️ Проблема

Если ключ передавать вручную, это неудобно и небезопасно — особенно при большом количестве хостов и серверов в сети. При этом компрометация одного ключа может раскрыть все последующие (если новый шифруется старым).

### ▪️ Решение — автоматическое распределение ключей через KDC

**KDC (Key Distribution Center)** — это центральный сервер, который управляет выдачей ключей между участниками сети.

* Каждый участник имеет **постоянный (permanent) ключ**, общий с KDC.
* Когда два участника (A и B) хотят обменяться данными:

  1. A → KDC: запрос соединения с B (по зашифрованному каналу).
  2. KDC генерирует **сеансовый ключ (session key)** — одноразовый ключ для конкретной сессии.
  3. KDC шифрует этот ключ постоянными ключами A и B и отправляет им.
  4. A и B теперь могут общаться, шифруя сообщения этим сеансовым ключом.

**Типы ключей:**

* **Permanent key** — долговременный секрет между участником и KDC.
* **Session key** — одноразовый ключ для текущей сессии; уничтожается после завершения соединения.

**Преимущества:**

* Централизованный контроль доступа.
* Динамическая выдача ключей.
* Гибкость при большом числе хостов и пользователей.

**Недостатки:**

* KDC — единая точка отказа.
* Необходим защищённый канал между KDC и участниками.

---

## 🔹 4.2 Kerberos — централизованная аутентификация пользователей

**Идея:** предоставить надёжный способ аутентификации в распределённой сети, не передавая пароли по сети. Основан на симметричном шифровании.

Kerberos использует **KDC**, разделённый на два логических сервера:

* **AS (Authentication Server)** — проверяет пароль пользователя и выдаёт билет для обращения к TGS.
* **TGS (Ticket-Granting Server)** — выдаёт билеты для доступа к конкретным сервисам.

### ▪️ Проблемы, которые решает Kerberos

1. Пользователь может подделать чужой ID на рабочей станции.
2. Можно изменить сетевой адрес машины и притвориться другим клиентом.
3. Возможна **replay-атака** (перехват и повтор старого запроса).

### ▪️ Основные участники

| Символ | Значение                                |
| :----: | :-------------------------------------- |
|    C   | Клиент (пользователь)                   |
|   AS   | Authentication Server                   |
|   TGS  | Ticket-Granting Server                  |
|    V   | Сервер-сервис (например, почта, печать) |
|   Kx   | Ключ, общий для пары участников         |

---

## 🔸 Kerberos Version 4

### 🔹 Общая логика работы

1. Пользователь вводит пароль, клиент формирует запрос к **AS** с указанием ID пользователя и ID сервера **TGS**.
2. **AS** проверяет пароль и возвращает:

   * **Ticket-granting ticket (TGT)** — билет для обращения к TGS.
   * **Сеансовый ключ Kc,tgs**, зашифрованный ключом из пароля пользователя **Kc**.
3. Клиент расшифровывает ответ с помощью пароля, получает билет и ключ. Теперь пароль больше не нужен.
4. Для получения доступа к какому-либо серверу V:

   * Клиент → TGS: отправляет билет **TGT** и запрос на сервис **V**.
   * TGS проверяет билет, создаёт новый билет **Ticketv** и ключ **Kc,v**.
5. Клиент → сервер V: отправляет **Ticketv** и одноразовый **Authenticator**, зашифрованный **Kc,v**.
6. Сервер проверяет билет и аутентификатор. При взаимной аутентификации сервер отвечает шифрованным временем `TS+1`.

---

### 🔹 Что решают билеты и аутентификаторы

* **Ticket** (билет) — безопасная «пропускная карточка», шифрованная ключом сервера. Клиент не может её подделать.
* **Authenticator** — доказательство того, что клиент действительно владеет ключом сейчас (включает ID и метку времени).
* **TS и Lifetime** — защита от replay.
* **ADC (адрес клиента)** — предотвращает использование билета с другого устройства.

---

### 🔹 Поток сообщений (версия 4)

```
(1) C → AS: IDc, IDtgs, TS1
(2) AS → C: E(Kc, [Kc,tgs, IDtgs, Lifetime, Tickettgs])
(3) C → TGS: IDv, Tickettgs, Authenticatorc = E(Kc,tgs, [IDc, ADC, TS3])
(4) TGS → C: E(Kc,tgs, [Kc,v, Ticketv])
(5) C → V: Ticketv, Authenticatorc = E(Kc,v, [IDc, ADC, TS5])
(6) V → C: E(Kc,v, [TS5+1])   # mutual authentication
```

**После шага (6)** клиент и сервер разделяют общий `Kc,v` и могут шифровать свои сообщения.

---

## 🔸 Kerberos Version 5 — улучшения (RFC 4120)

**Цель:** устранить ограничения и уязвимости версии 4.

### 🔹 Основные улучшения

| Проблема в v4                             | Решение в v5                                                 |
| ----------------------------------------- | ------------------------------------------------------------ |
| Зависимость от DES                        | Поддержка любого шифра (идентификатор алгоритма).            |
| Только IP-адреса                          | Любые типы адресов (ISO, IPX, и т.д.).                       |
| Ограничение времени жизни (макс. ~21 час) | Явные поля `from`, `till`, `renew-till` для гибких сроков.   |
| Нестандартный PCBC-режим                  | Стандартный CBC + отдельная целостность (checksum/hash).     |
| Replay через старый билет                 | Возможность установить **subsession key** для каждой сессии. |
| Отсутствие делегирования                  | Введены **forwardable/proxiable tickets**.                   |
| Межреалм требует N² связей                | Поддержка цепочек доверия между реалмами.                    |
| Возможен офлайн-брутфорс пароля           | Добавлен механизм **pre-authentication**.                    |

---

### 🔹 Поток сообщений (v5)

```
(1) C → AS: Options, IDc, Realm, IDtgs, Times, Nonce1
(2) AS → C: E(Kc, [Kc,tgs, Times, Nonce1, IDtgs, Tickettgs])
(3) C → TGS: Options, IDv, Times, Nonce2, Tickettgs, Authenticatorc
(4) TGS → C: E(Kc,tgs, [Kc,v, Times, Nonce2, IDv, Ticketv])
(5) C → V: Options, Ticketv, Authenticatorc
(6) V → C: E(Kc,v, [TS2, Subkey?, Seq#?])
```

**Новое:**

* `Subkey` — отдельный ключ для конкретного приложения.
* `Seq#` — нумерация сообщений (защита от повторов/перестановок).
* `Options` — флаги (например, `forwardable`, `renewable`, `proxiable`).

---

### 🔹 Преимущества Kerberos v5

* Гибкость в алгоритмах и адресах.
* Повышенная защита от replay и брутфорса.
* Возможность делегирования и межреалмовой аутентификации.
* Стандартный формат (ASN.1/BER) для кроссплатформенной совместимости.

---

## 🔹 Выводы по разделам

* **KDC** — основа автоматической выдачи ключей, обеспечивает масштабируемость.
* **Kerberos** — практическая реализация модели KDC с тикетами и симметричным шифрованием.
* **Версия 5** — современный стандарт (RFC 4120), устраняющий ограничения DES-эпохи.

---

## 🔹 4.3 — Key Distribution Using Asymmetric Encryption   

---

### 🎯 Цель  
Понять, как публичное (асимметричное) шифрование решает проблему безопасного распределения ключей,  
а также как используются **публичные сертификаты (X.509)** и **сеансовые ключи**.

---

## 💡 Идея  
Асимметричное шифрование используется для двух целей:  
1. **Распределение публичных ключей.**  
2. **Использование публичных ключей для передачи секретных (сеансовых)** ключей.

---

## ⚠️ Проблема  

Если просто публиковать ключи в открытом доступе, любой злоумышленник может:  
- выдать себя за другого пользователя;  
- подделать «публичный ключ»;  
- перехватывать сообщения, предназначенные настоящему владельцу.  

То есть **невозможно быть уверенным**, что полученный публичный ключ действительно принадлежит тому, кому нужно.

---

## 🔐 Решение — Public-Key Certificates  

### 📘 Что такое сертификат  
**Публичный сертификат** = публичный ключ + идентификатор владельца,  
подписанные **доверенным центром сертификации (CA — Certificate Authority)**.  

### 📋 Процесс выдачи сертификата:  
1. Пользователь передаёт свой публичный ключ центру сертификации (CA).  
2. CA проверяет личность владельца.  
3. CA создаёт **хэш** данных и **подписывает** его своей приватной подписью.  
4. Сертификат публикуется.  
5. Любой может проверить подлинность с помощью **публичного ключа CA**.

> 💡 Таким образом, никто не может подделать чужой публичный ключ,  
> а доверие обеспечивается цифровой подписью CA.

---

### 🧩 Стандарт X.509  
Это международный формат сертификатов.  
Используется в:  
- **SSL / TLS** (HTTPS-сайты)  
- **IPsec**  
- **S/MIME (электронная почта)**  
- **SET (Secure Electronic Transactions)**  

---

## 🔑 Public-Key Distribution of Secret Keys  

### 📘 Проблема симметричного обмена  
При симметричном шифровании участникам нужно заранее обменяться секретным ключом.  
Но если они находятся в разных странах, безопасно передать ключ практически невозможно.

---

### 🧠 Пример без асимметрии  
> Боб и Алиса хотят обмениваться сообщениями.  
> Чтобы использовать AES, им нужен общий секретный ключ.  
> Но пересылка ключа по сети может привести к его перехвату.

---

### 🧮 Решения  

#### 1️⃣ Diffie–Hellman Key Exchange  
Позволяет двум сторонам создать общий секрет через открытый канал.  
❌ Недостаток — **нет аутентификации**,  
невозможно быть уверенным, что ключ сгенерирован именно с нужным участником.  

#### 2️⃣ Public-Key Certificates  
Использование сертификатов позволяет безопасно обмениваться ключами  
и быть уверенным в подлинности собеседника.

---

## 📦 Пример обмена (Боб ↔ Алиса)

1. **Боб** готовит сообщение.  
2. **Шифрует сообщение** симметричным алгоритмом (например, AES)  
   с **одноразовым сеансовым ключом**.  
3. **Шифрует сеансовый ключ** с помощью **публичного ключа Алисы**.  
4. Отправляет Алисе:  
   - зашифрованное сообщение,  
   - зашифрованный сеансовый ключ.  
5. **Алиса** расшифровывает сеансовый ключ своим приватным ключом  
   и дешифрует сообщение.  

> ✅ Если Боб получил публичный ключ из **сертификата Алисы**,  
> он уверен, что ключ подлинный и принадлежит именно ей.

---

## 🧱 Итог  

| Этап | Что решает | Механизм |
|------|-------------|----------|
| Распространение публичных ключей | Проверка подлинности ключей | Public-Key Certificate (X.509) |
| Обмен секретными ключами | Безопасная передача сеансовых ключей | Public-key encryption (RSA) |
| Доверие к источнику | Гарантия, что ключ настоящий | Certificate Authority (CA) |

---

📘 **Вывод:**  
Асимметричное шифрование не заменяет симметричное —  
оно решает главную задачу: **как безопасно передать ключи** и  
**удостовериться в личности участников** с помощью инфраструктуры доверия (CA и X.509).

## 🔹 Глава 4.4 — X.509 Certificates   

---

### 🎯 Цель  
Понять, что такое сертификаты **X.509**, как они создаются, хранятся, проверяются,  
а также как работает цепочка доверия и отзыва сертификатов.

---

## 💡 Идея  
**X.509** — это стандарт от ITU-T (часть серии X.500),  
который определяет формат **публичных ключевых сертификатов**  
и методы **аутентификации** пользователей в распределённой сети.  

Он используется во многих протоколах безопасности:  
- **SSL / TLS** (HTTPS)  
- **IPsec**  
- **S/MIME**  
- **SET (Secure Electronic Transactions)**  

---

## 🏗️ Структура X.509  

### 📘 Что это такое  
X.509 описывает **рамочную систему** аутентификации,  
в которой каждый пользователь имеет сертификат,  
подписанный **центром сертификации (CA)**.  
Эти сертификаты хранятся в каталоге (directory service),  
который действует как база данных доверенных ключей.

---

## 🧱 Формат сертификата (по Figure 4.4a)

Каждый сертификат содержит следующие поля:

| Поле | Назначение |
|------|-------------|
| **Version** | Определяет версию формата (1, 2 или 3). Версия 3 добавила расширения. |
| **Serial number** | Уникальный номер сертификата внутри CA. |
| **Signature algorithm ID** | Алгоритм, использованный для подписи сертификата. |
| **Issuer name** | Имя центра сертификации (CA), выдавшего сертификат. |
| **Period of validity** | Срок действия — “с” и “по”. |
| **Subject name** | Имя владельца сертификата (чей это ключ). |
| **Subject’s public key info** | Сам публичный ключ владельца и его параметры. |
| **Issuer unique ID / Subject unique ID** | (опционально) — уникальные идентификаторы CA и владельца, если их имена использовались повторно. |
| **Extensions** | (только версия 3) — дополнительные поля, описанные ниже. |
| **Signature** | Подпись CA, покрывающая весь сертификат. |

---

## 🔏 Принцип подписи сертификата

1. CA создаёт сертификат:  
   → включает публичный ключ, имя владельца и параметры.  
2. Вычисляет **хэш всего содержимого**.  
3. **Шифрует хэш своим приватным ключом** — это и есть **цифровая подпись**.  
4. Получатель проверяет подпись с помощью публичного ключа CA.

> 💡 Сертификат нельзя подделать:  
> любое изменение данных приведёт к несоответствию подписи.

---

## 📬 Получение сертификатов

- Любой пользователь, зная **публичный ключ CA**, может проверить подлинность сертификата.  
- Каталог (directory) лишь **хранит** сертификаты, но **не создаёт и не проверяет** их.  
- Сертификаты можно размещать в открытом доступе — они безопасны для чтения.  

---

## 🔗 Цепочки доверия (Certificate Chains)

Если все пользователи используют одного CA — всё просто.  
Но в реальности существует множество CA, и пользователи должны уметь  
**доверять сертификатам, выданным другими центрами**.

### Пример:
- Пользователь **A** доверяет **CA X₁**.  
- Пользователь **B** имеет сертификат, выданный **CA X₂**.  

Чтобы A доверял B:
1. A получает из каталога сертификат **X₂**, подписанный **X₁**.  
2. Проверяет подпись через X₁.  
3. Теперь A знает публичный ключ X₂.  
4. A получает сертификат B, подписанный X₂,  
   и проверяет его с помощью ключа X₂.  

→ A получил доверие к B через цепочку:  
X₁ → X₂ → B

Такая цепочка может быть длинной:  
X₁ → X₂ → X₃ → ... → Xₙ → B

---

## 🌳 Иерархия центров сертификации (CA Hierarchy)

Чтобы облегчить проверку, X.509 предлагает строить **иерархию CA** (Figure 4.6):  
- Каждый CA может иметь **forward certificates** (сертификаты, выданные ему другими CA).  
- И **reverse certificates** (сертификаты, выданные им другим CA).  

> Пример пути:  
> ```
> XW → WV → VY → YZ → ZB
> ```  
> Пользователь A, получив эти сертификаты, может извлечь публичный ключ B  
> и начать безопасное шифрованное общение.

---

## 🚫 Отзыв сертификатов (Certificate Revocation)

У каждого сертификата есть срок действия,  
но иногда требуется **досрочно аннулировать** его.

Причины:
1. Компрометация приватного ключа.  
2. Пользователь больше не сертифицирован (смена имени, ошибка, политика).  
3. Компрометация CA.

### 🔁 CRL — Certificate Revocation List
Каждый CA ведёт **список отозванных сертификатов**,  
включая и пользовательские, и другие CA.  

Список содержит:
- Имя издателя.  
- Дату создания и следующего обновления.  
- Серийные номера и даты отзыва.  
- Подпись CA.  

> Пользователи могут проверять локальные копии CRL,  
> чтобы не обращаться каждый раз к каталогу.

---

## 🧩 X.509 Version 3  

Версия 3 добавила **расширения (extensions)** —  
гибкую систему дополнительных полей,  
которые можно добавлять по мере необходимости.  

Каждое расширение содержит:
- **Extension ID** — идентификатор.  
- **Criticality** — флаг критичности (если `TRUE`, нельзя игнорировать).  
- **Value** — значение расширения.

---

### 📗 Категории расширений  

#### 1️⃣ Key and Policy Information  
Дополнительные сведения о ключах и политике сертификата.

- **Authority key identifier** — указывает ключ CA, которым подписан сертификат.  
- **Subject key identifier** — уникально определяет ключ владельца.  
- **Key usage** — назначение ключа (подпись, шифрование, обмен и т.д.).  
- **Private-key usage period** — срок действия приватного ключа.  
- **Certificate policies** — перечень поддерживаемых политик.  
- **Policy mappings** — сопоставление политик между CA.

---

#### 2️⃣ Certificate Subject and Issuer Attributes  
Передают дополнительные данные о владельце или издателе сертификата.  

- **Subject alternative name** — альтернативные имена (например, e-mail, IP, DNS).  
- **Issuer alternative name** — альтернативные имена CA.  
- **Subject directory attributes** — почтовый адрес, должность, изображение и т.п.

---

#### 3️⃣ Certification Path Constraints  
Ограничения для сертификатов, выданных другими CA.

- **Basic constraints** — можно ли субъекту быть CA и максимальная длина цепочки.  
- **Name constraints** — допустимые доменные имена для последующих сертификатов.  
- **Policy constraints** — необходимость явного указания политики  
  или запрет её подмены в цепочке.

---

## 🧱 Итог  

| Элемент | Назначение |
|----------|-------------|
| X.509 Directory | Хранилище сертификатов и CRL |
| Certificate Authority (CA) | Центр, выдающий и подписывающий сертификаты |
| Chain of Trust | Цепочка доверенных CA, обеспечивающая валидацию ключей |
| CRL | Механизм отзыва скомпрометированных сертификатов |
| Extensions | Гибкие дополнительные поля в версии 3 |

---

📘 **Вывод:**  
X.509 — это основа всей инфраструктуры доверия (PKI).  
Он определяет, как **сертификаты создаются, проверяются и отзываются**,  
и как строится **иерархия доверия** между пользователями и центрами сертификации.  
Без X.509 невозможно представить современные протоколы безопасности —  
от HTTPS до электронной подписи.

## 🔐 Глава 4.5 — Public-Key Infrastructure (PKI)

---

### 🎯 Цель

Понять, что такое **инфраструктура открытых ключей (PKI)**,  
какие роли в ней участвуют, какие функции управления выполняются  
и как модель **PKIX** (X.509) организует выпуск, хранение и отзыв сертификатов.

---

### 💡 Идея

**PKI** — это:
> «железо + софт + люди + политики + процедуры»,  
которые нужны, чтобы **создавать, управлять, распространять и отзывать**  
цифровые сертификаты на основе асимметричной криптографии.

Главная цель:  
✅ сделать получение и проверку **открытых ключей** безопасным и удобным.

Модель **PKIX (Public Key Infrastructure X.509)** от IETF:
- формальная архитектура PKI, основанная на **X.509**;
- определяет роли, потоки управления и протоколы.

---

### 🧱 Основные сущности PKIX

- **End Entity (конечная сущность)**  
  Пользователь, сервер, роутер и т.п. — тот, на кого выдаётся сертификат.  
  Использует услуги PKI (подпись, шифрование, аутентификация).

- **Certification Authority (CA)**  
  Центр сертификации:  
  - выпускает сертификаты;  
  - обычно публикует **CRL** (списки отозванных сертификатов);  
  - может выполнять часть админ-функций.

- **Registration Authority (RA)**  
  Доп. роль, на которую CA может делегировать админку:  
  - проверка личности перед выдачей сертификата;  
  - участие в регистрации, изменении данных и др.

- **CRL Issuer**  
  Сущность, которой CA может поручить **публикацию CRL**.

- **Repository (репозиторий)**  
  Хранилище сертификатов и CRL, доступное конечным сущностям  
  (LDAP, HTTP-директория и т.п.).

---

### 🛠 PKIX Management Functions (функции управления)

1. **Registration (регистрация)**  
   Первичное знакомство пользователя с CA/RA.  
   - проходит аутентификация (онлайн/офлайн);  
   - могут выдаваться общие секреты для последующего общения.

2. **Initialization (инициализация)**  
   Подготовка клиента к работе:  
   - установка доверенных ключей CA;  
   - настройка параметров для проверки цепочек сертификатов.

3. **Certification (сертификация)**  
   CA:
   - подписывает открытый ключ пользователя → **сертификат**;  
   - возвращает его пользователю и/или публикует в репозитории.

4. **Key Pair Recovery (восстановление пары ключей)**  
   Для ключей, которые используются для **расшифрования**:  
   - предусмотрен механизм восстановления (backup у CA/ключевого сервиса);  
   - нужен на случай утери пароля, поломки токена и т.п.

5. **Key Pair Update (обновление ключей)**  
   Регулярная замена ключей и выпуск новых сертификатов:  
   - по окончании срока действия;  
   - при компрометации или других причинах.

6. **Revocation Request (запрос на отзыв)**  
   Уполномоченное лицо сообщает CA, что сертификат нужно отозвать:  
   - компрометация приватного ключа;  
   - смена организации;  
   - изменение имени и т.д.  
   CA обновляет CRL/OCSP-ответы.

7. **Cross Certification (кросс-сертификация)**  
   - два CA обмениваются инфой и выпускают **cross-certificate** друг для друга;  
   - это позволяет строить цепочки доверия между разными PKI.

---

### 📡 PKIX Management Protocols

- **CMP (Certificate Management Protocol, RFC 2510)**  
  - гибкий протокол, где каждая функция управления вынесена в отдельный обмен;  
  - покрывает все этапы: от регистрации до отзыва.

- **CMC (Certificate Management Messages over CMS, RFC 2797)**  
  - использует существующий формат **CMS (Cryptographic Message Syntax)**;  
  - также поддерживает функции PKIX, но не всегда «одна функция = один обмен»;  
  - сделан, чтобы переиспользовать уже реализованные механизмы CMS.

---

## 🌐 Глава 4.6 — Federated Identity Management

---

### 🎯 Цель

Разобраться, как **единая схема управления идентичностями**  
может работать сразу в нескольких организациях/доменах,  
давая пользователю **SSO через границы компаний**.

---

### 💡 Идея

**Identity Management (IdM)** — централизованный способ:
- задавать цифровую личность пользователя (principal);  
- привязывать к ней **атрибуты**;  
- обеспечивать проверку личности и контроль доступа.

Ключевая концепция: **Single Sign-On (SSO)**  
> один раз аутентифицировался → доступ ко всем ресурсам в домене.

---

### 🧱 Элементы системы IdM

- **Authentication** — проверка, что пользователь = указанному имени.  
- **Authorization** — выдача прав на основе аутентификации и атрибутов.  
- **Accounting** — логирование действий/доступа.  
- **Provisioning** — добавление пользователей в систему.  
- **Workflow automation** — автоматизация бизнес-процессов (движение данных).  
- **Delegated administration** — RBAC, управление правами через роли.  
- **Password synchronization** — реализация SSO/RSO.  
- **Self-service password reset** — пользователь сам меняет пароль.  
- **Federation** — передача результата аутентификации и прав в другие системы.

> Kerberos уже реализует многие из этих функций.

---

### 🧩 Архитектура Identity Management

Основные сущности:

- **Principal** — носитель идентичности (пользователь, устройство, процесс).  
- **Identity Provider (IdP)** — аутентифицирует principal,  
  хранит идентификаторы и атрибуты.  
- **Attribute Service** — создаёт и поддерживает доп. атрибуты  
  (адрес, роли, права, информация сотрудника).  
- **Data Consumer / Service Provider (SP)** — потребитель данных IdP/Attribute Service (БД-сервер, файловый сервер и т.п.), использует атрибуты для авторизации.

Идея:  
✅ пользователь один раз сообщает свои данные и поддерживает их в одном месте;  
✅ сервисы запрашивают только нужные атрибуты согласно политике доступа/приватности.

---

### 🌍 Identity Federation

**Identity federation** = IdM, расширенный на несколько **безцентрализованных доменов**:  
внутренние подразделения, партнёры, поставщики, внешние сервисы.

Цель:
- пользователь аутентифицируется **один раз** в своём домене;  
- дальше с этой идентичностью получает доступ к ресурсам в других доменах.

Особенности:
- нет единого центра управления — только **соглашения и доверие**;  
- организации договариваются о:
  - формате идентичностей и атрибутов,  
  - протоколах обмена,  
  - правилах маппинга, логирования, аудита.

Функции федерации:
- **SSO за пределами одного домена**;  
- стандартные форматы атрибутов (роли, аккаунты, местоположение, владение файлами);  
- **identity mapping** — преобразование идентификаторов/атрибутов  
  под требования другого домена.

---

### 🔄 Общая схема работы федерации (рис. 4.9)
<img width="943" height="757" alt="image" src="https://github.com/user-attachments/assets/75287f71-563f-40dd-a41a-e337d228d524" />

1. Пользователь аутентифицируется у **своего IdP** и задаёт/обновляет атрибуты.  
2. Администратор может добавлять атрибуты (роль, права, служебная инфа).  
3. **Service Provider** в другом домене запрашивает у IdP:  
   - информацию об идентичности;  
   - факт аутентификации;  
   - нужные атрибуты.  
4. SP открывает сессию с пользователем и применяет **политику доступа**.

---

### 📜 Стандарты федерации

- **SAML (Security Assertion Markup Language)**  
  - основной стандарт;  
  - передаёт **assertions** об объекте (subject):  
    - факт аутентификации,  
    - атрибуты,  
    - авторизационную инфу.

- **OASIS WS-Federation**  
  - федерация для веб-сервисов/браузеров;  
  - использует **Security Token Service (STS)**, который выдает доверенные токены.

Задачи стандартов:
- описать формат «билетов» (security tickets);  
- протоколы обмена;  
- управление: настройка маппинга атрибутов, логирование, аудит.

---

### 🧪 Примеры сценариев федерации (рис. 4.10)
<img width="1254" height="862" alt="image" src="https://github.com/user-attachments/assets/4b1684c4-75f3-4bb6-b966-4931ceee5ee1" />

1. **(a) Federation based on account linking**  
   - Workplace.com ↔ Health.com.  
   - Пользователь логинится на Workplace, браузер редиректится на Health.  
   - Workplace передаёт **идентификатор пользователя**,  
     Health имеет свои учётки для каждого сотрудника.  
   - Связь по **аккаунтам**, браузер-ориентированный сценарий.

2. **(b) Federation based on roles**  
   - Workplace.com ↔ PartsSupplier.com.  
   - Пользователь аутентифицирован на Workplace и имеет роль (Engineer / Purchaser).  
   - При переходе на PartsSupplier доступ даётся **по роли**,  
     не по конкретному аккаунту пользователя в этой системе.  
   - Связь по **ролям**, а не по индивидуальной записи.

3. **(c) Chained Web Services (документ-ориентированный сценарий)**  
   - Workplace.com → PinSupplies.com → E-Ship.com.  
   - Пользователь логинится и оформляет заказ через procurement-приложение.  
   - Приложение формирует **XML/SOAP** сообщение:  
     - в header — учётные данные пользователя и идентичность организации;  
     - в body — заказ.  
   - PinSupplies аутентифицирует сообщение, затем отправляет SOAP в E-Ship  
     с собственным токеном + данными по доставке.  
   - E-Ship аутентифицирует запрос и оформляет доставку.  


